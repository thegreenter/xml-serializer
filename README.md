# xml serializer

Demo de serializar XML **Invoice UBL 2.1**

## Ejecutar
El resultado será un archivo `invoice.xml`.

```
composer install
vendor/bin/phpunit 
```
Para generar los metadatos de otros comprobantes, revisar esta [guía](/generator).
